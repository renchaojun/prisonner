<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
 <mapper namespace="a.b">
 	<select id="selIds1"  resultType="long">
		SELECT p.code from subcopapi p inner join infotesttotal i on p.code=i.subtableid inner join jbxx_ziranxinxi y on y.zf_bh=i.clientinfoid;
 	</select>
 	<select id="selIds2"  resultType="long">
		SELECT p.code from subcopapi p inner join infotesttotal i on p.code=i.subtableid inner join yzgl_zfjfkhxx y on y.zf_bh=i.clientinfoid;
 	</select>
 	
</mapper>